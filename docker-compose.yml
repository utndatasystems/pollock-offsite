services:
  mariadb-server:
    image: mariadb:10.9.3
    container_name: mariadb-server
    environment:
      - MYSQL_USER=benchmark
      - MYSQL_PASSWORD=benchmark
      - MYSQL_ROOT_PASSWORD=benchmark
      - MYSQL_DATABASE=benchmark
      - MYSQL_TCP_PORT=3307
      - MYSQL_UNIX_PORT=3307
    ports:
      - "3307:3307"
    volumes:
      - mariadb-tmp:/tmp
    networks:
      mariadb-nw:
  mariadb-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/mariadb/Dockerfile
    volumes:
      - ./results/mariadb:/results/mariadb
      - ./data:/data
      - ./sut/mariadb/:/app/
      - ./sut/utils.py:/app/utils.py
      - mariadb-tmp:/tmp
    depends_on:
      - mariadb-server
    networks:
      mariadb-nw:
  mysql-server:
    image: mysql:8.0.31
    container_name: mysql-server
    environment:
      - MYSQL_USER=benchmark
      - MYSQL_PASSWORD=benchmark
      - MYSQL_ROOT_PASSWORD=benchmark
      - MYSQL_DATABASE=benchmark
    command:
      - '--local-infile=1'
      - '--secure-file-priv=/tmp'
    ports:
      - "3336:3336"
    volumes:
      - mysql-tmp:/tmp
    networks:
      mysql-nw:
  mysql-client:
    build:
      context: .
      dockerfile: ./sut/mysql/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/mysql:/results/mysql
      - ./data:/data
      - mysql-tmp:/tmp
      - ./sut/mysql/:/app/
      - ./sut/utils.py:/app/utils.py
    depends_on:
      - mysql-server
    networks:
      mysql-nw:
  postgres-server:
    image: postgres:15.0
    container_name: postgres-server
    environment:
      - POSTGRES_USER=benchmark
      - POSTGRES_PASSWORD=benchmark
      - POSTGRES_DB=benchmark
    ports:
      - "5442:5442"
    volumes:
      - postgres-tmp:/tmp
      - ./results/postgres:/results/postgres
      - ./data:/data
    networks:
      postgres-nw:
  postgres-client:
    build:
      context: .
      dockerfile: ./sut/postgres/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - postgres-tmp:/tmp
      - ./results/postgres:/results/postgres
      - ./data:/data
      - ./sut/utils.py:/app/utils.py
      - ./sut/postgres/:/app/
    depends_on:
      - postgres-server
    networks:
      postgres-nw:
  sqlite-client:
    build:
      context: .
      dockerfile: ./sut/sqlite/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/sqlite:/results/sqlite
      - ./data:/data
      - ./sut/sqlite/:/app/
      - ./sut/utils.py:/app/utils.py
    networks:
      sqlite-nw:
  pandas-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/pandas/Dockerfile
    volumes:
      - ./results/pandas:/app/results/pandas
      - ./data:/app/data
      - ./sut/pandas/:/app/
      - ./sut/utils.py:/app/utils.py
  duckdbparse-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/duckdbparse/Dockerfile
    volumes:
      - ./results/duckdbparse:/app/results/duckdbparse
      - ./data:/app/data
      - ./sut/duckdbparse/:/app/
      - ./sut/utils.py:/app/utils.py
  duckdbauto-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/duckdbauto/Dockerfile
    volumes:
      - ./results/duckdbauto:/app/results/duckdbauto
      - ./data:/app/data
      - ./sut/duckdbauto/:/app/
      - ./sut/utils.py:/app/utils.py
  opencsv-client:
    build:
      context: .
      dockerfile: ./sut/opencsv/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/opencsv:/results/opencsv
      - ./data:/data
      - ./sut/opencsv/:/app/
  pycsv-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/pycsv/Dockerfile
    volumes:
      - ./results/pycsv:/app/results/pycsv
      - ./data:/app/data
      - ./sut/pycsv/:/app/
      - ./sut/utils.py:/app/utils.py
  rcsv-client:
    build:
      context: .
      dockerfile: ./sut/rcsv/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/rcsv:/results/rcsv
      - ./data:/data
      - ./sut/rcsv/:/app/
  rhypoparsr-client:
    build:
      context: .
      dockerfile: ./sut/rhypoparsr/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/rhypoparsr:/results/rhypoparsr
      - ./data:/data
      - ./sut/rhypoparsr/:/app/
  clevercs-client:
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    build:
      context: .
      dockerfile: ./sut/clevercs/Dockerfile
    volumes:
      - ./results/clevercs:/app/results/clevercs
      - ./data:/app/data
      - ./sut/clevercs/:/app/
      - ./sut/utils.py:/app/utils.py
  libreoffice-client:
    build:
      context: .
      dockerfile: ./sut/libreoffice/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/libreoffice:/results/libreoffice
      - ./data:/data
      - ./sut/utils.py:/app/utils.py
      - ./sut/libreoffice/:/app/
  univocity-client:
    build:
      context: .
      dockerfile: ./sut/univocity/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/univocity:/results/univocity
      - ./data:/data
      - ./sut/univocity/:/app/
  csvcommons-client:
    build:
      context: .
      dockerfile: ./sut/csvcommons/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/csvcommons:/results/csvcommons
      - ./data:/data
      - ./sut/csvcommons/:/app/
  custom-client:
    build:
      context: .
      dockerfile: ./sut/custom/Dockerfile
    environment:
      - DATASET=${DATASET}
      - N_REPETITIONS=${N_REPETITIONS}
    volumes:
      - ./results/custom:/results/custom
      - ./data:/data
      - ./sut/custom/:/app/
      - ./sut/utils.py:/app/utils.py
  pollution:
    image: pollock_pollution
    command: ["python3", "/app/pollute_main.py"]
    build:
      context: .
      dockerfile: Dockerfile-pollution
    volumes:
      - ./data:/app/data
      - ./results/source.csv:/app/results/source.csv
  evaluate:
    image: evaluate
    build:
      context: .
      dockerfile: Dockerfile-pollution
    environment:
      - DATASET=${DATASET}
    command: ["python3", "/app/evaluate.py"]
    volumes:
      - ./results:/app/results
      - ./data:/app/data
      - ./evaluate.py:/app/evaluate.py
      - ./sut/utils.py:/app/utils.py
  paper-tables:
    image: paper_tables
    build:
      context: .
      dockerfile: Dockerfile-pollution
    environment:
      - DATASET=${DATASET}
    command: ["python3", "/app/paper_tables.py"]
    volumes:
      - ./results:/app/results
      - ./data:/app/data
      - ./paper_tables.py:/app/paper_tables.py
      - ./sut/utils.py:/app/utils.py

volumes:
  mariadb-tmp:
  mysql-tmp:
  postgres-tmp:

networks:
  mariadb-nw:
  mysql-nw:
  postgres-nw:
  sqlite-nw:
