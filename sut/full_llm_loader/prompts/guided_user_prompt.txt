Reconstruct the following CSV-like artifact.

Return exactly two sections and nothing else.
Do not use Markdown fences. Do not include prose before, between, or after the sections.
The section markers must appear exactly as shown, each on its own line.

--BEGIN FIXED CSV--
<complete valid CSV text only, including exactly one flattened output header row>
--END FIXED CSV--
--BEGIN ERROR REPORT--
<JSON report object only>
--END ERROR REPORT--

Rules for the fixed CSV section:

- Use a comma as the output delimiter.
- Use double quotes according to standard CSV escaping rules.
- Encode an embedded double quote as two double quotes.
- Preserve embedded line breaks inside quoted fields.
- Use LF line endings.
- Include exactly one flattened header row.
- Do not wrap the CSV in Markdown fences.
- Do not include comments, explanations, or section labels inside the CSV.
- If the file contains multiple independent tables, reconstruct the primary table only and document excluded tables in the report.
- If no table can be reconstructed from non-empty evidence, return only a header row with positional column names that best match the evidence, and document the uncertainty in the report.
- If the input is empty, whitespace-only, or contains no table evidence, leave the fixed CSV section empty. Do not invent headers, rows, columns, examples, placeholder data, or sample content.

Use this JSON schema for the error report:

{
  "input_assessment": {
    "detected_encoding": "<encoding or unknown>",
    "candidate_delimiters": ["<delimiter>"],
    "selected_structure": "<brief description>",
    "table_count": 0
  },
  "line_classification": [
    {
      "start_line": 1,
      "end_line": 1,
      "role": "preamble|metadata|header|data|section_header|footer|blank|unresolved",
      "reason": "<brief evidence-based explanation>"
    }
  ],
  "repairs": [
    {
      "type": "<repair type>",
      "source_lines": [1],
      "description": "<what structural or syntactic repair was made>",
      "evidence": "<file-internal evidence supporting the repair>",
      "payload_changed": false
    }
  ],
  "excluded_content": [
    {
      "source_lines": [1],
      "classification": "preamble|metadata|section_header|footer|comment|other",
      "content_summary": "<brief summary>",
      "reason": "<why it was not included as table data>"
    }
  ],
  "ambiguities": [
    {
      "source_lines": [1],
      "alternatives": ["<alternative reconstruction>"],
      "selected": "<selected reconstruction>",
      "reason": "<why it was preferred>",
      "confidence": "high|medium|low"
    }
  ],
  "validation": {
    "rectangular_table": true,
    "valid_csv_syntax": true,
    "source_data_rows_accounted_for": true,
    "no_invented_values": true,
    "no_silent_payload_changes": true,
    "warnings": []
  },
  "summary": "<concise description of the reconstruction>"
}

Additional output constraints:

- Use physical input line numbers, starting at 1.
- Combine adjacent lines with the same role into one line_classification entry.
- Do not report ordinary CSV quoting as a repair.
- Set payload_changed to true only when a source value itself was changed, not when quoting, escaping, delimiter placement, header flattening, or table separation changed.
- Any payload_changed=true repair must also produce a validation warning.
- If the reconstruction is uncertain, still return the best defensible result, but record the uncertainty.
- Do not include analysis, prose, Markdown, or comments outside the two required sections.

CSV-LIKE ARTIFACT:

{{CSV_TEXT}}
